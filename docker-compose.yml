version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: learn-sphere-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=learnsphere

volumes:
  mongo_data:
    driver: local
